# CLAUDE.md 분리 계획표

## Context (왜 나누는가)

현재 `CLAUDE.md`는 548줄, 약 4만 단어 분량의 단일 파일이다. 사건 배경·DB 구조·보안 프로토콜·문서 작성 규칙·검증 등급 체계·사고 이력이 전부 한 파일에 누적되어 있고, 매 세션 시작 시 전체가 로드된다. 대통령이 "너무 길어서 느려지고 에러가 나는 것 같다"고 지적했다 — 파일이 커질수록 세션 시작 비용과 오류 가능성이 늘어난다.

목표는 **"매 세션 자동으로 꼭 필요한 것"**과 **"특정 작업을 할 때만 펼쳐보면 되는 것"**을 분리하는 것이다. 이 구분이 이번 계획의 핵심 설계 기준이다:

- **CLAUDE.md에 남는 것** = 대통령이 무슨 말을 하든(또는 새 대화가 열리는 순간) 항상 자동으로 작동해야 하는 규칙 — 조직 정체성, 비서실장 응대 형식, 매 응답 강제 브리핑(`v_intake_status`), 관리자 입장 판별, 기록 발동어 트리거. 이런 것들은 "필요할 때 문서를 펼쳐본다"는 전제가 성립하지 않는다 — **항상** 작동해야 하므로 시작할 때 이미 읽혀 있어야 한다.
- **docs/로 옮기는 것** = 특정 종류의 작업(법률문서 작성, DB 구조 확인, 발송물 검증, 세팅모드 전환 등)을 할 때만 참조하면 되는 절차·SQL·체크리스트·배경 설명. CLAUDE.md에는 "이런 상황이면 docs/0N을 먼저 읽어라"는 **색인 한 줄**만 남긴다.
- 사고 이력("유래" 문단)은 **07_INCIDENT_HISTORY.md로 전문 이관 + 원래 자리엔 요약 포인터만** 남긴다 (예: `> 유래: 2026.7.26 — 발송본 186건 감사 9회전 사고. 전문: docs/07_INCIDENT_HISTORY.md#2026-07-26`).
- **규칙의 의미는 어떤 경우에도 삭제하지 않는다** — 위치만 옮기거나, "요약+전문 링크"로 나눌 뿐이다.
- 보안상 민감한 값(비밀번호·열쇠 등)은 원래 CLAUDE.md에도 없었으므로 이관 시에도 노출되지 않는다 — 별도 마스킹 작업 불필요, 그대로 옮기면 됨.

이 문서는 **계획표만**이다. 실제 파일 분할·CLAUDE.md 수정은 이 계획이 승인된 뒤 별도 작업으로 진행한다. 이번 단계에서는 이 계획표 파일 하나만 저장소에 생성하며, `CLAUDE.md` 자체는 수정하지 않고 커밋·PR도 하지 않는다.

---

## 새 CLAUDE.md 골격 (목표 약 150~180줄)

승인 후 실제 CLAUDE.md에 남길 내용의 뼈대는 다음과 같다:

1. 제목 + 1줄 소개
2. 🎯 설립 배경과 목적 — **축약판**: 목적 선언문 3~4줄 + 실장 조직 한 줄. 금고 296건의 성격별 세부 수치(기관원본/제3자원본/가공본/작업본 건수)와 그 유래는 `docs/01_PROJECT_OVERVIEW.md`로 이동 (수치는 어차피 "조회로 재확인" 대상이라 CLAUDE.md에 고정 텍스트로 있을 이유가 약하다).
3. 보안 구도 한 줄 (`GitHub=배포 전용 / Supabase=금고`) — 다른 여러 규칙이 이 전제를 인용하므로 짧게 남김.
4. **📚 문서 색인** (신설) — "이런 작업을 할 때는 이 문서를 먼저 읽어라" 표. 아래 "새 문서 목록" 절 참고.
5. 🏛️ 조직 체계 — 조직도 + 실장 역할 전체 유지.
6. 🗂️ 비서실장 상시 응대 규칙 — 전체 유지 (매 응답 형식 트리거).
7. 📌 대통령 접속 자동 브리핑 — 전체 유지, 단 `v_intake_status`/`v_deadline` 등 뷰가 정확히 무엇을 보여주는지의 **설명**은 `docs/03_DATABASE_MAP.md`로, 여기는 "언제 무엇을 조회해서 어떻게 보고하는지" 절차만.
8. 🙋 관리자 입장 프로토콜 — 트리거 문구·브리핑 항목 리스트는 유지(대화 시작 시점 판별이 핵심이라 지연 로드 불가), 환경 건강 점검 체크리스트 상세는 `docs/02_SECURITY_RULES.md`로.
9. 🛡️ 관리자 활동 감사 기록 — 전체 유지(이미 짧고, 감사 의무이므로 누락 방지 위해 상시 노출 필요).
10. 📥 반입대장 강제 점검 — "지켜야 할 것 (예외 없음)" 리스트는 유지(매 응답 강제 규범), `intake`/`기록유형` 컬럼 설계 배경과 상세 SQL은 `docs/03_DATABASE_MAP.md`·`docs/04_WORKFLOW_RULES.md`로.
11. ⭐ 대화 기록 규칙 — 발동어·발동 시점·완전성 원칙·요약/상세 2층 구분·category 확인 절차는 유지(언제든 발동될 수 있는 트리거), INSERT SQL 템플릿·채번 쿼리는 `docs/04_WORKFLOW_RULES.md`로 이동하고 "SQL 정확한 형식은 docs/04 참고"로 대체.
12. 🎫 토큰 관리 규칙 — "주간 한도 신호등"과 "무거운 작업 전 확인" 두 규칙만 축약 유지, 나머지(서브에이전트 고정비 실측, Workflow 스키마 주의사항 등)는 `docs/04_WORKFLOW_RULES.md`로.

나머지 전 섹션(Supabase 인프라 상세, 세팅모드, 네트워크장애대응, 원본불변, 코워크경계, 법률문서/문서산출 지침, 사실관계검증, 작업등급·검증체계 전체, 정기구조공시)은 아래 표대로 docs/로 이동하고 CLAUDE.md에는 색인 한 줄만 남긴다.

---

## 새 문서 목록과 색인 문구(안)

| 파일 | 읽어야 할 시점 | CLAUDE.md에 남길 색인 문구(예) |
|---|---|---|
| `docs/01_PROJECT_OVERVIEW.md` | 사건 배경·금고 수치 상세가 필요할 때 | "사건 배경·금고 자료 성격별 상세 수치는 `docs/01_PROJECT_OVERVIEW.md` 참고" |
| `docs/02_SECURITY_RULES.md` | 세팅모드 전환·네트워크 장애·코워크 연동·관리자 계정 관련 작업 시 | "세팅모드/네트워크 장애/원본 불변/코워크 경계 규칙은 `docs/02_SECURITY_RULES.md` 참고 — 착수 전 필독" |
| `docs/03_DATABASE_MAP.md` | Supabase 테이블·뷰 구조를 확인해야 할 때 | "DB 테이블·뷰 전체 목록·용도는 `docs/03_DATABASE_MAP.md` 참고" |
| `docs/04_WORKFLOW_RULES.md` | 반입대장 SQL, 기록 INSERT 템플릿, 토큰 관리 실무, 정기 구조 공시 시 | "반입대장·기록 저장 SQL 템플릿, 토큰 관리 실무는 `docs/04_WORKFLOW_RULES.md` 참고" |
| `docs/05_DOCUMENT_OUTPUT_RULES.md` | 법률문서·외부 발송 문서를 작성할 때 | "법률문서 작성/외부 발송물 준비 전 `docs/05_DOCUMENT_OUTPUT_RULES.md` 필독" |
| `docs/06_VERIFICATION_RULES.md` | 사실관계 판독·등급 판정·감사 절차가 필요할 때 | "원문 판독·사실 확정·감사 절차(작업 등급표 포함)는 `docs/06_VERIFICATION_RULES.md` 필독 — 2·3등급 작업 착수 전 확인" |
| `docs/07_INCIDENT_HISTORY.md` | 특정 규칙의 배경 사고를 자세히 봐야 할 때 | "각 규칙의 사고 이력 전문은 `docs/07_INCIDENT_HISTORY.md` (규칙 옆 유래 포인터에서 링크)" |

`docs/관리자-런북.md`는 기존 그대로 유지(이번 분리 대상 아님, 참조만 유지).

---

## 섹션별 이동 매핑표 (원본 CLAUDE.md 548줄 전체)

| # | 원본 섹션 (현재 줄번호) | 이동先 | 처리 방식 |
|---|---|---|---|
| 1 | 제목·소개 (1-3) | CLAUDE.md 유지 | 그대로 |
| 2 | 🎯 설립 배경과 목적 (5-20) | CLAUDE.md 축약 유지 + `docs/01` | 목적 선언문·조직 개요는 CLAUDE.md에, 296건 성격별 세부 수치·괄호 유래("2026.7.30 감사 실측...")는 `docs/01`로 |
| 3 | 웹페이지/보안구도/자동공개/파일구조 (22-28) | CLAUDE.md 축약 + `docs/01` | 보안구도 한 줄만 CLAUDE.md 유지, 나머지(빌드도구 없음 등 세부)는 `docs/01` |
| 4 | 🗄️ Supabase 인프라 전체 (30-63) | `docs/03_DATABASE_MAP.md` | 전문 이동. `posts`/`case_evidence`/`evidence_class`/`v_sendable`/`v_unclassified`/`category_meta`/`evidence_text`/`fact`·`issue`/`statute`/`disposition`·`deadline_critical`·`fact_conflict`·`v_axis_board`/가입차단·`registration_status`/`app_settings` 전부 포함. 유래 문단은 요약 포인터로 대체, 전문은 `docs/07`행 추가 |
| 5 | 🔧 세팅 모드 규칙 (65-75) | `docs/02_SECURITY_RULES.md` | 전문 이동 (발동 문구 "세팅끝"/"세팅시작"과 SQL·효과·수동대체 절차 포함) |
| 6 | 🔌 커넥터/네트워크 장애 대응 프로토콜 (77-97) | `docs/02_SECURITY_RULES.md` | 전문 이동 |
| 7 | 🔒 원본 불변 원칙 (99-103) | `docs/02_SECURITY_RULES.md` | 전문 이동 |
| 8 | 🧱 코워크 ↔ 코드 권한 경계 (105-125) | `docs/02_SECURITY_RULES.md` | 전문 이동 |
| 9 | ⚖️ 법률문서 작성 프로토콜 (127-161) | `docs/05_DOCUMENT_OUTPUT_RULES.md` | 전문 이동 (0단계 쟁점 시작·작업순서·조문 인용 규칙·조문 등재 3원칙 포함). 각 유래 문단은 포인터로 축약 |
| 10 | 📄 문서 산출 지침 (163-214) | `docs/05_DOCUMENT_OUTPUT_RULES.md` | 전문 이동 (수신자 구분·4중 검사·산출→감사→승인 순서·미감사초안 원칙·기계검사 고정목록·칸 확인·원본만 발송·압축본 특칙 포함) |
| 11 | 🛡️ 사실관계 보고 검증 지침 (216-226) | `docs/06_VERIFICATION_RULES.md` | 전문 이동 |
| 12 | 🎚️ 작업 등급·검증 단계 규칙 전체 (228-354) — 등급판정 3문답, 등급별 배정표, 강제 체크리스트 0~10, 정정 자가검증, 내부교정 검증, 회귀테스트, 위험오독쌍 경보, 고위험 이중판독, 서브에이전트 활동정지 판정, 감사 종료조건 | `docs/06_VERIFICATION_RULES.md` | 전문 이동. 이 절이 가장 크므로 `docs/06` 내부에서 소제목 구조는 그대로 유지(등급판정 → 배정표 → 체크리스트 → 정정/교정검증 → 회귀테스트 → 오독경보 → 이중판독 → 활동정지판정 → 종료조건) |
| 13 | 🎫 토큰 관리 규칙 (356-378) | CLAUDE.md 축약(2줄) + `docs/04_WORKFLOW_RULES.md` | "잔여 20% 미만이면 신규 서브에이전트/Workflow 금지" 핵심 한 줄만 CLAUDE.md, 실시간 표시 방법·모델 설정·고정비 실측치는 `docs/04` |
| 14 | 📐 정기 구조 공시 (380-384) | `docs/04_WORKFLOW_RULES.md` | 전문 이동 |
| 15 | 🏛️ 조직 체계 (386-405) | CLAUDE.md 유지 | 그대로 (조직도·실장 역할) |
| 16 | 🗂️ 비서실장 상시 응대 규칙 (407-417) | CLAUDE.md 유지 | 그대로 |
| 17 | 📥 반입대장 강제 점검 (419-454) | CLAUDE.md(핵심 리스트) + `docs/03`·`docs/04` | "지켜야 할 것 1~8" 리스트와 매 응답 첫 줄 보고 의무는 CLAUDE.md 유지. `intake`/`기록유형` 컬럼 설계·뷰 설명은 `docs/03`, 유래 전문은 `docs/07`(포인터만 CLAUDE.md/03에 남김) |
| 18 | 📌 대통령 접속 자동 브리핑 (456-468) | CLAUDE.md 유지 | 그대로 (트리거 절차이므로 지연 로드 불가) |
| 19 | 🙋 관리자 입장 프로토콜 (470-494) | CLAUDE.md(트리거+브리핑 항목) + `docs/02` | 입장 인식·브리핑 항목 리스트·차단 안내·권한 한계는 CLAUDE.md 유지. "환경 건강 점검" 상세 체크리스트는 `docs/02` |
| 20 | 🛡️ 관리자 활동 감사 기록 (496-506) | CLAUDE.md 유지 | 그대로 (감사 의무, 짧음) |
| 21 | ⭐ 대화 기록 규칙 (508-548) | CLAUDE.md(트리거+원칙) + `docs/04` | 발동어·발동시점·완전성원칙·요약/상세 구분·category 확인 절차는 CLAUDE.md 유지. INSERT SQL 템플릿·채번 쿼리·dept/tags 세부 규칙은 `docs/04` |

---

## 유래(사고 이력) 문단 처리 방식 (확정)

각 규칙 문서(02~06)에 붙어 있던 "> 유래: ..." 전문은 `docs/07_INCIDENT_HISTORY.md`로 옮기고, 원래 자리에는 요약 포인터 한 줄만 남긴다.

예시 (`docs/06_VERIFICATION_RULES.md` 안):

```
### 감사 종료 조건
- 연속 2회 치명적 결함 0이고, 서로 다른 관점(전건 기계검사/표본 렌더링 육안 판독)일 것.
- 상한: 5회차 넘어도 결함 계속되면 감사 중단, 대통령 보고.
> 유래: 2026.7.26 — 발송본 186건 감사 9회전 사고. 전문: docs/07_INCIDENT_HISTORY.md#2026-07-26
```

`docs/07_INCIDENT_HISTORY.md`는 날짜순으로 각 사고의 전문(경위·발견 경과·시정 내용)을 담고, 각 항목 앵커(`#YYYY-MM-DD`)가 원 규칙 문서의 포인터와 정확히 일치해야 한다.

---

## 검증 방법 (분리 실행 시 반드시 지킬 것)

실제 분리 작업(이번 계획표 승인 후 별도 단계)을 수행할 때는:
1. 원본 CLAUDE.md의 각 문장이 정확히 어느 새 파일로 이동했는지 추적 가능해야 한다 — 이동 전후 문장 수(또는 핵심 키워드) 대조.
2. 이동 후 원본 CLAUDE.md 문구 중 다른 곳(예: `docs/관리자-런북.md`, `index.html` 등)에서 상대경로로 참조되던 문구가 있는지 `grep -rn`으로 확인.
3. 색인 문구(위 "새 문서 목록" 표)가 실제로 CLAUDE.md에 전부 반영됐는지 확인.
4. `docs/07_INCIDENT_HISTORY.md`의 각 사고 항목이 원 규칙 문서의 포인터(`docs/07_INCIDENT_HISTORY.md#YYYY-MM-DD`)와 앵커가 실제로 일치하는지 확인.
5. 분리 후에도 규칙 총량(의미 단위)이 줄지 않았는지 — 새 문서 7개 + 축약 CLAUDE.md의 총 내용이 원본 548줄의 모든 의미를 포함하는지 사람이 한 번 대조 확인.

---

## 이번 단계에서 한 것 / 하지 않은 것

- **했음**: 이 계획표 파일(`CLAUDE_MD_REFACTOR_PLAN.md`) 저장소 루트에 생성.
- **하지 않음**: `CLAUDE.md` 수정, `docs/01~07` 파일 생성, 커밋, PR 생성 — 전부 대통령의 별도 승인이 필요한 다음 단계.
