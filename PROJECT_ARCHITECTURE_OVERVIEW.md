# PROJECT ARCHITECTURE OVERVIEW

> 이 문서는 외부 AI(ChatGPT 등)의 아키텍처 검토를 위해 **일반화**하여 작성되었습니다.
> 실제 데이터베이스 테이블/컬럼명, 조직 명칭, 증거 식별 체계, 사건 당사자·기관의 실명은
> 보안 정책상 이 문서에 포함하지 않았습니다. 아래는 **구조·흐름·설계 패턴**에 대한 설명이며,
> 실제 운영 데이터는 별도의 접근 통제된 백엔드에만 존재합니다.

---

## 1. 프로젝트 개요

### 1.1 목적
한 개인이 행정기관을 상대로 장기간 진행 중인 행정적·법적 대응(정보공개청구, 이의신청, 행정심판/소송, 감사 청구 등)을 체계적으로 뒷받침하기 위한 **개인용 증거관리·논증생성·기록관리 시스템**이다. 목표는 다음 세 가지로 요약된다.

1. **증거의 무결성 보전** — 원본 문서를 변경 없이 보관하고, 그로부터 파생된 모든 가공물(요약·발췌·논증)을 원본과 명확히 분리해 추적한다.
2. **주장의 근거 기반화** — 모든 사실 주장은 원문 인용과 출처가 있어야 하며, 추측이나 창작된 문구가 법률문서에 섞이지 않도록 기계적으로 강제한다.
3. **운영 연속성** — 방대한 원본 자료(수만 쪽)를 매번 전부 다시 읽지 않고도, 새 문서를 빠르게 조립할 수 있는 구조를 유지한다.

### 1.2 현재 해결하려는 문제
- 원본 자료량이 많아(수백 건, 수만 쪽) 매 문서 작성 시 전량 재검토가 비현실적이다.
- AI가 원문을 잘못 읽거나(오독), 존재하지 않는 문구를 있는 것처럼 인용하는(환각) 위험이 실제로 여러 차례 발생했다.
- 여러 대상(행정기관·법률대리인·감사기관·언론 등)에 맞춰 같은 사실관계를 다른 문체·형식으로 반복 가공해야 한다.
- 외부로 나가는 문서에 내부 작업 흔적(경로, 도구명, 내부 메모, 미검증 초안)이 섞여 나가면 안 된다.

### 1.3 전체 운영 방식 (요약)
```
[원본 자료 반입] → [판독(OCR/텍스트추출/육안대조)] → [개별 사실 확정(근거·인용문 첨부)]
  → [사실들을 묶어 쟁점화] → [쟁점 기반으로 대상별 문서 조립] → [독립 검증(자기검증 금지)]
  → [승인] → [발송/제출] → [원본 그대로 기록 보존]
```
핵심 설계 원칙은 **"원문은 한 번만 읽고, 그로부터 확정된 사실을 여러 번 재사용한다"**는 것이다. 이를 통해 대상(행정기관·법률대리인·감사기관 등)이 늘어나도 원문을 다시 읽는 비용은 늘지 않는다.

---

## 2. 폴더 구조

정적 웹사이트(빌드 도구 없음, 순수 HTML/JS)와 자동화 스크립트, 내부 문서로 구성된다.

```
project-root/
├── index.html              # 게시판 (일일 기록 목록 표시)
├── org.html                 # 조직도 시각화
├── (상황판).html            # 사건 진행 상황판 (KPI·쟁점 현황 대시보드)
├── (타임라인).html          # 처분·사실·기한을 시간순으로 병합한 타임라인 뷰
├── auth.js                  # 로그인 게이트 (백엔드 인증 연동)
├── theme.css                # 공용 디자인 토큰 + 컴포넌트 스타일
├── nav.js                   # 페이지 간 공용 네비게이션 (페이지 목록 단일 소스)
│
├── .github/
│   └── workflows/
│       ├── auto-publish.yml     # 작업 브랜치 push → 회귀테스트 → main 자동 병합
│       ├── deploy-pages.yml     # main → 정적 페이지 배포 (명시적 파일 화이트리스트)
│       └── keepalive-*.yml      # 백엔드 무료 플랜 자동 정지 방지용 정기 핑
│
├── scripts/
│   └── site/
│       └── test-site.sh         # 배포 전 회귀 테스트 스크립트 (CI 게이트)
│
├── docs/
│   └── (관리자 운영 문서)        # 기술 트러블슈팅·복구 절차 (비공개, 배포 제외)
│
└── (프로젝트 설정 문서)          # 조직/운영 규칙, 프로토콜 정의 (비공개, 배포 제외)
```

### 폴더별 역할
| 폴더/파일군 | 역할 |
|---|---|
| 루트 `*.html`/`*.js`/`*.css` | 사용자가 브라우저에서 보는 화면 전체. 로그인 후에만 데이터가 로드됨 |
| `.github/workflows/` | 코드 변경 시 자동 검증·배포 파이프라인 |
| `scripts/site/` | 배포 전 기계적 회귀 검증(페이지 목록 일치, 핵심 함수 단위테스트 등) |
| `docs/` | 사람(운영자)이 읽는 절차 문서. 웹에 배포되지 않음 |
| 프로젝트 설정 문서 | AI 에이전트가 매 세션 시작 시 로드하는 운영 규칙 전문(全文). 웹에 배포되지 않음 |

**중요한 설계 결정**: 저장소 루트를 통째로 배포하지 않는다. 배포 대상은 **명시적 화이트리스트**(파일명 배열)로 관리하며, 배포 직전 "예상 파일 수 == 실제 파일 수"를 자동 검증한다. 이는 스크래치 파일이나 내부 문서가 실수로 공개되는 사고를 막기 위한 세 번째 개선판 설계다(아래 9장 참고).

---

## 3. 파일 구조

### 핵심 파일과 용도

| 파일 | 용도 | 상호 관계 |
|---|---|---|
| `index.html` | 일일 기록(활동 로그) 게시판. 카테고리별 탭·색상 표시 | 백엔드의 "게시글" 테이블 + "카테고리 표시설정" 테이블을 읽어 동적 렌더링 |
| `org.html` | 조직 구조도 + 부서별 기록 건수 시각화 | 게시글 테이블의 소유 부서(dept) 값을 집계 |
| 상황판.html | 핵심 지표(KPI) 스트립 + 처분 현황 + 쟁점 준비도 요약 | 여러 뷰(view)를 병렬 조회 후 배지·경고 형태로 렌더링 |
| 타임라인.html | 처분·확정사실·기한을 하나의 연대기로 병합 | 3개 테이블을 병합·정렬하여 "오늘" 마커와 함께 표시 |
| `auth.js` | 로그인 게이트 공용 모듈 | 모든 페이지 상단에서 호출, 인증 실패 시 화면 접근 자체를 차단 |
| `nav.js` | 네비게이션 메뉴 단일 소스 | 페이지 목록이 하나의 배열로 정의되어 있어, 새 페이지 추가 시 여기만 고치면 전체 반영 |
| `theme.css` | 색상 변수·공용 컴포넌트(카드·배지·표) | 라이트/다크 테마를 CSS 변수로 분기 |
| `test-site.sh` | 배포 전 검증 스크립트 | 자동배포 파이프라인 두 곳(병합 전/배포 시)에서 모두 호출됨 |

### 파일 간 관계
- **`nav.js`가 페이지 목록의 유일한 정의처**다. 배포 워크플로의 화이트리스트, 네비게이션 메뉴, 회귀테스트가 이 목록을 서로 대조해 불일치를 자동 검출한다.
- **`theme.css`는 모든 화면이 공유**하며, 페이지별 `<style>`에는 그 페이지 고유 레이아웃만 남긴다.
- **HTML 파일들은 빌드 과정 없이 브라우저에서 직접 백엔드 API를 호출**한다. 즉 "프론트엔드 코드"와 "실행되는 코드"가 동일 파일이다(번들링·트랜스파일 없음).

---

## 4. 데이터 흐름

### 4.1 원본 자료가 들어오는 과정
1. 운영자(사건 당사자)가 파일을 업로드하거나, 외부 포털(행정 정보공개 시스템 등)에서 받은 문서를 전달한다.
2. AI 세션 시작 시점에 **"반입 점검" 자동 훅**이 실행되어, 이번에 새로 들어온 파일들의 지문(해시값)을 계산하고 기존 대장과 대조한다.
3. 대장에 없는(=아직 등재되지 않은) 파일은 **판독 전에 먼저 등재**한다 — "읽기 전에 존재를 기록"하는 순서를 강제해, 파일이 조용히 누락되는 것을 막는다.

### 4.2 PDF/문서 처리 과정 (3단 사다리 방식)
비용이 싼 방법부터 순서대로 시도하고, 실패해야 다음 단계로 넘어간다.
```
1단계: 텍스트 레이어 추출 (가장 저렴, 초 단위)
   ↓ (추출 실패/글자 깨짐/30자 미만이면)
2단계: 이미지로 렌더링 후 육안 판독 (AI가 이미지를 직접 봄)
   ↓ (그래도 불확실하면)
3단계: 고해상도로 재렌더링 후 재판독 (1회 한정)
```
"판독 불가"라는 결론은 이 3단계를 실제로 다 시도한 뒤에만 허용된다 — 텍스트 추출이 실패했다고 곧바로 "못 읽었다"고 보고하는 것은 금지되어 있다(과거 실제로 이 규칙이 없어 렌더링하면 읽히는 문서를 "판독 불가"로 오보고한 사고가 있었다).

### 4.3 AI 분석 과정
판독된 원문은 그대로 저장되는 것이 아니라, **3개 계층**으로 구조화된다.

```
[1계층] 원문 캐시        — 판독한 텍스트를 쪽 단위로 그대로 저장 (가공 없음)
    ↓
[2계층] 개별 사실(Fact)  — "누가, 언제, 무엇을 했다"는 단위로 사실을 하나씩 확정.
                          반드시 원문 인용문 + 출처 + 확인한 쪽수를 동반해야 "확정" 상태가 됨
    ↓
[3계층] 쟁점(Issue)      — 여러 개별 사실을 조합해 "이러이러하므로 위법/부당하다"는
                          하나의 논증 단위로 묶음
```

이 구조를 만든 이유는, 원본 5만 쪽을 전부 재검토하지 않고도 "지금 주장 가능한 쟁점이 몇 개인지", "어떤 사실이 더 확인돼야 하는지"를 즉시 조회할 수 있게 하기 위해서다. 새 문서(행정기관용/법률대리인용/감사기관용 등)를 만들 때는 **원문을 다시 읽지 않고 이 3계층에서 조립**하는 것이 원칙이다.

### 4.4 검토·검증 과정
- 개별 사실을 "확정" 상태로 만들려면, 근거로 인용한 문구가 실제로 원문 캐시(1계층)에 문자열 그대로 존재하는지를 **자동 함수가 재확인**한다. 일치하지 않으면 "확정" 자체가 데이터베이스 제약조건에 의해 거부된다(사람이 실수로 건너뛸 수 없도록 구조적으로 강제).
- 금액·기한·법조문처럼 한 글자만 틀려도 결론이 바뀌는 고위험 인용문은, 서로 다른 시점에 독립적으로 두 번 읽어 일치할 때만 "확정"이 허용된다.
- 과거 실제로 발생했던 오독 패턴(자음이 비슷한 단어를 다른 단어로 잘못 읽는 등)을 사전에 등록해두고, 새 판독에서 그 단어가 등장하면 자동으로 경고가 뜨는 장치도 있다.

### 4.5 데이터 저장 과정
모든 자료는 인증 기반 접근제어가 걸린 백엔드 데이터베이스에만 저장된다. 정적 웹사이트 코드 저장소에는 **화면을 그리는 코드만** 있고, 실제 자료나 대화 기록은 전혀 포함되지 않는다(아래 6장 참고).

### 4.6 최종 문서 생성 과정
```
쟁점 선택 → 관련 개별 사실 인용 → 대상별 문체 적용 → 초안 작성
  → "미검증 초안" 표식 부착 → 독립 검증(작성자와 분리된 별도 검토) → 결함 발견 시 수정 후 재검증
  → 대통령(의사결정권자) 승인 → 발송/제출 → 발송 이력 및 전문을 다시 기록에 보존
```

---

## 5. AI 처리 방식

### 5.1 어떤 단계에서 AI가 자료를 읽는가
- **1차 판독**: 신규 반입 자료를 3단 사다리 방식(4.2절)으로 읽어 원문 캐시를 만드는 단계.
- **사실 확정**: 원문에서 특정 사실을 뽑아 정형화하는 단계. 여기서부터는 "판독"이 아니라 "사실관계 확정"으로 취급되어 더 높은 등급의 모델과 더 엄격한 검증이 요구된다.
- **문서 초안 작성**: 이미 확정된 사실·쟁점을 조합해 새 문서를 쓰는 단계. 원문을 다시 읽지 않는 것이 원칙.
- **독립 검증**: 작성자와 분리된 별도의 AI 실행 인스턴스가, 작성자의 결론을 신뢰하지 않고 원본부터 다시 대조하는 단계.

### 5.2 OCR 또는 문서 분석 방식
- 기본은 PDF의 텍스트 레이어 추출(전통적 텍스트 파싱)이며, 실패 시에만 이미지 렌더링 후 **AI의 시각 인식(vision)** 으로 전환한다.
- 별도의 OCR 엔진(Tesseract 등)도 시험됐으나, 표·양식이 많은 공문서 특성상 인식률이 낮아 **"두 번째 독립 판독"으로는 채택하지 않았다** — 두 번째 판독은 반드시 사람 또는 AI가 다시 눈으로 읽는 방식이어야 한다는 결론에 도달했다.

### 5.3 검토·검증 과정 (거버넌스)
작업의 무게에 따라 4단계 등급을 사전에 정의해두고, 등급별로 투입 모델·검증 횟수를 다르게 한다.

| 등급 | 작업 성격 | 검증 |
|---|---|---|
| 0 | 단순 응대 | 없음 |
| 1 | 기록·목록 작성 | 자기 점검 1회 |
| 2 | 원문 판독·수치 산정·사실 확정 | 자기 점검 + 별도 인스턴스의 독립 판독 1회 |
| 3 | 외부로 나가는 문서 | 자기 점검 + 독립 검증(연속 2회 결함 없음 확인될 때까지 반복, 5회 초과 시 방식 자체를 재검토) |

3등급 문서의 독립 검증은 다음 5개 항목으로 고정된 보고 형식을 따른다: **판정 / 치명적 결함 / 주의사항 / 실제로 대조한 표본 내역 / 이번에 확인하지 못한 항목**. 특히 "확인하지 못한 항목"을 솔직히 밝히도록 강제하는 것이 핵심이다 — 검증자가 "괜찮다"고만 말하고 한계를 숨기는 것을 막기 위함이다.

### 5.4 새로운 문서 생성 과정
- 문체·형식은 수신 대상(행정기관 공문서체/법률대리인 앞 법률문서체/감사기관 청구서체/일반인 대상 평이체 등)에 맞춰 조정하되, **인용하는 사실 자체는 동일한 확정 사실 데이터에서만** 가져온다.
- 외부로 나가는 모든 초안은 완성 즉시 상단에 "미검증 초안 — 제출 금지" 표식을 붙이며, 이 표식이 없는 초안을 의사결정권자에게 보여주는 것 자체가 금지되어 있다.
- 발송 전 최종적으로 **④단계 기계 검사**를 거친다: ①수신자 적합성(내부 전략이 새지 않았는지) ②렌더링 부산물(자동 삽입된 경로·페이지 번호 등) ③고정된 금지어 목록에 대한 문자열 검사(내부 조직명·시스템 용어·내부 식별자가 남아있지 않은지) ④수신자가 실제로 열었을 때 읽을 수 있는 형태인지(파일명 규칙, 목록과 실제 첨부의 1:1 대응 등).

---

## 6. 백엔드 데이터베이스 구조

### 6.1 사용 목적
정적 웹사이트가 다루는 **모든 실질 데이터**(원문 판독 결과, 확정된 사실, 쟁점, 활동 기록, 기한 관리 등)를 인증 기반으로 통제된 하나의 백엔드(PostgreSQL 기반 BaaS 플랫폼)에 모은다.

### 6.2 저장하는 데이터 종류 (일반화)
- **반입 대장**: 들어온 자료가 판독됐는지, 어떤 조치가 필요한지, 기한이 있는지 추적하는 표.
- **원문 캐시**: 판독된 문서의 쪽 단위 텍스트.
- **판독 결과 요약**: 문서 단위 요약본과, 그 신뢰도(추출 방식·확인 비율 기반으로 등급이 자동 계산됨).
- **개별 사실 대장**: 위 4.3절의 2계층. 근거·인용문·확정 여부를 포함.
- **쟁점 대장**: 위 4.3절의 3계층. 법적 근거, 필요 사실 목록, 반박 위험 등을 포함.
- **처분/기한 대장**: 시간이 지나면 권리가 소멸하는 불가역 기한(제소기간 등)을 별도 추적.
- **법령 축자 대장**: 법조문 원문을 출처와 함께 보관 — 요약이 아닌 원문만 실제 문서에 인용 가능하도록 강제.
- **활동 기록(게시판) 데이터**: 날짜별 작업 로그.
- **접근 계정 관리**: 등록 가능한 계정 수를 데이터베이스 트리거로 원천 제한.

### 6.3 테이블 구조 (설계 패턴만 서술)
- 테이블은 "원본 불변 원칙"에 따라 **원본 성격의 테이블(수정·삭제 금지, 조회만 가능)**과, **그 원본에 대한 분류·검증 결과를 별도로 쌓는 테이블**로 분리되어 있다. 즉 원본 테이블을 직접 고치는 대신, "이 원본의 성격은 무엇이다"라는 태그를 별도 테이블에 얹는 방식이다.
- 데이터베이스 자체의 **제약조건(CHECK)** 을 이용해, "근거 없이 확정 상태로 저장하는 것"이 애초에 불가능하도록 만들어 두었다(응용 코드의 실수로 우회될 수 없음).
- 조회 전용 뷰(view)를 다수 정의해, "지금 당장 판독이 필요한 것", "지금 바로 주장 가능한 쟁점", "기한이 임박한 것" 등을 매번 원시 테이블을 조합하지 않고 즉시 조회할 수 있게 했다.
- 신뢰 등급(A/B/C)은 사람이 직접 입력하는 값이 아니라, "판독 방식 + 확인 비율 + 본문 분량" 조건으로부터 **자동 계산되는 값**이다 — 등급이 "선언"이 아니라 "실재 여부"로 매겨지도록 하기 위함이다.

### 6.4 데이터 입력·조회 방식
- 입력은 AI 에이전트가 백엔드 API(SQL 실행 도구)를 통해 직접 수행한다. 별도의 관리자 화면(어드민 패널)은 없다.
- 조회는 두 경로다: (a) 정적 웹사이트가 로그인 사용자에게 브라우저에서 직접 API를 호출해 보여주는 경로, (b) AI 에이전트가 세션 중 직접 조회하는 경로.
- 접근 제어는 행 단위 보안 정책(Row-Level Security)으로 구현되어, 인증되지 않은 요청은 어떤 테이블도 읽을 수 없다.

### 6.5 정적 사이트 저장소(Git)와 백엔드의 연결 구조
```
[사용자 브라우저]
     │  (로그인)
     ▼
[정적 HTML/JS] ──── API 호출(인증 토큰 포함) ────▶ [백엔드 DB/Storage]
     ▲
     │  배포
[코드 저장소(Git)] → [자동 배포 파이프라인] → [정적 페이지 호스팅]
```
- 코드 저장소에는 **자료가 전혀 없다**. 화면을 그리는 HTML/JS/CSS와 배포 파이프라인 설정만 있다.
- 정적 페이지가 브라우저에서 실행될 때 비로소 백엔드에 API를 호출해 데이터를 가져온다. 즉 "배포된 코드"와 "실제 자료"는 물리적으로 완전히 분리된 두 개의 시스템이다.
- 백엔드 접속 정보(공개 가능한 클라이언트 키)만 정적 코드에 포함되며, 그 키로는 로그인하지 않은 상태에서 아무 데이터도 읽을 수 없다(접근제어 정책이 서버 측에 있음).

---

## 7. Git(코드 저장소)의 역할

### 7.1 관리 대상
- **화면 코드만** 관리한다: HTML/CSS/JS, 배포 파이프라인 설정, 회귀 테스트 스크립트, (배포되지 않는) 운영 문서.
- 실제 자료, 대화 기록, 판독 결과, 사건 관련 문서는 **일절 포함되지 않는다**. 이는 이 프로젝트의 최상위 보안 원칙이다.

### 7.2 버전 관리 방식
- 표준 Git 브랜치 모델: 작업 브랜치에 커밋 → 자동화된 검증(회귀 테스트) 통과 시 → 기본 브랜치로 자동 병합 → 배포.
- **사람의 수동 코드 리뷰 없이 자동 병합**되는 구조이기 때문에, 병합 전 게이트(자동 테스트)의 신뢰도가 특히 중요하게 설계되어 있다.

### 7.3 변경 이력 관리 방식
- 코드 변경 이력은 Git 커밋 로그로, **활동 이력(누가 언제 무엇을 했는가)은 백엔드의 게시판 데이터**로 완전히 분리해서 관리한다.
- 배포 대상 파일 목록은 코드 자체에 명시적 화이트리스트로 박혀 있어, 배포 시점마다 "지금 배포되는 파일이 몇 개인지"를 스스로 검증하고, 예상과 다르면 배포를 중단한다.

---

## 8. 문서 생성 과정 (전체 흐름 상세)

```
① 증거자료 PDF 반입
   └─ 지문(해시) 계산 → 반입 대장에 등재(아직 미판독 상태로)

② 분석(판독)
   └─ 텍스트 추출 시도 → 실패/불충분 시 이미지 렌더링 후 AI 육안 판독
   └─ 전체 쪽수 확인, 마지막 쪽까지 확인했는지 기록
   └─ 원문 쪽 단위 텍스트를 원문 캐시에 저장

③ 핵심 내용 추출
   └─ 원문에서 "사실"이 될 만한 문장을 특정 (누가·언제·무엇을)
   └─ 그 문장이 원문 캐시에 실제로 존재하는지 자동 대조
   └─ 통과하면 "확정" 사실로 등재 (실패 시 확정 자체가 시스템적으로 거부됨)

④ 논리 구조화
   └─ 확정된 개별 사실들을 조합해 "쟁점"(주장 단위)으로 묶음
   └─ 쟁점마다 법적 근거, 필요한 사실 목록, 예상 반박과 그 대응을 함께 기록
   └─ 필요한 사실이 전부 확정되면 "지금 즉시 주장 가능"으로 자동 분류됨

⑤ 새로운 문서 생성
   └─ 쟁점 + 확정 사실만을 재료로, 수신 대상에 맞는 문체로 초안 작성
   └─ 인용문은 원문 그대로만 사용 (창작·의역 금지)
   └─ 초안 상단에 "미검증" 표식 부착

⑥ 독립 검증
   └─ 작성자와 분리된 별도 실행 인스턴스가 처음부터 재검증
   └─ 결함 발견 시 ⑤로 되돌아가 수정 → 재검증 (연속 무결함 2회까지 반복)

⑦ 승인 및 발송
   └─ 검증 결과를 의사결정권자에게 먼저 보고 → 승인 후에만 실제 발송
   └─ 발송 직전 최종 기계 검사(수신자 적합성/내부 흔적/금지어/가독성)

⑧ 기록 보존
   └─ 발송된 문서 전문과 발송 명세를 다시 백엔드에 영구 보존
```

이 세션에서 실제로 겪은 사례(일반화): 어떤 청구 문서를 준비하는 과정에서 특정 논거를 뒷받침하려던 비교 대상 사례가, 사실은 관련 후속 판결로 이미 뒤집혀 있었다는 것이 4~5차 독립 검증 단계에서야 드러나 해당 논거 전체를 들어냈다. 이는 "초안 작성자의 자기 확신"과 "독립 검증자의 원문 재대조"가 왜 분리되어야 하는지를 보여주는 실제 사례다.

---

## 9. 현재 방식의 장점과 문제점

### 장점
- **재발 방지가 구조에 남는다**: 사고가 발생할 때마다 "다음엔 조심하자"가 아니라 데이터베이스 제약조건·자동 검사 스크립트로 못박아, 같은 유형의 실수가 기계적으로 재발할 수 없게 만든다.
- **원문 재검토 비용이 선형으로 늘지 않는다**: 사실·쟁점 계층 덕분에, 대상(문서 종류)이 늘어나도 원문을 다시 읽는 비용은 거의 늘지 않는다.
- **독립 검증이 실제로 결함을 잡아낸다**: 자기 검증만으로는 놓쳤던 문제(원문보다 과장된 결론, 산술 오류, 이미 뒤집힌 판례 인용, 개인정보 노출 등)를 별도 인스턴스의 재검증이 여러 차례 실제로 적발했다.

### 문제점 / 속도가 느린 부분
- **독립 검증 1회당 고정 비용이 크다**: 별도 실행 인스턴스를 새로 띄울 때마다 맥락을 처음부터 다시 구성해야 해서, 내용 난이도와 무관하게 상당한 고정 비용(토큰·시간)이 발생한다. 짧은 확인 작업 하나에도 이 비용이 그대로 붙는다.
- **결함 수정 후 처음부터 재검증**하는 구조라, 결함이 여러 차례 나오면 검증 횟수가 누적된다(이번 세션에서 하나의 문서에 대해 다섯 차례 독립 검증이 필요했던 사례가 있었다).
- **반복 작업**: 같은 유형의 신뢰도 확인(원문 이미지 재대조), 같은 형식의 감사 보고서 작성이 문서마다 반복된다.
- **하부 시스템 접근성 제약**: 이번 세션처럼 실행 환경에 따라 저장소(자료 원본 파일)에 직접 접근하지 못하는 경우, 사실 확정의 최종 단계(원문 이미지 육안 재검증)를 완결하지 못하고 대기 상태로 남는 사례가 있었다.
- **일부 원본 자료의 신뢰 등급이 낮게 머물러 있다**: 오래전에 텍스트 추출로만 처리되고 아직 이미지 재검증을 거치지 않은 자료가 존재하며, 이런 자료는 실제 문서에 인용하기 전 재작업이 필요하다.

### 개선 가능한 부분
- 독립 검증의 고정 비용을 낮추는 방향(예: 검증 대상만 발췌해 전달, 맥락 재구성 최소화).
- 원문 재검증이 막힌 경우의 대체 경로(원본 저장소 접근 방법 다변화) 마련.
- 반복되는 검증 보고서 형식을 템플릿화해 매번 처음부터 설계하지 않도록.

---

## 10. 향후 개선 방향

### DB화 필요 영역
- 아직 활동 로그(게시판)에만 산발적으로 남아있는 의사결정 이력 일부를, 검색 가능한 정형 데이터로 옮길 필요가 있다.
- 법조문 축자 대장이 아직 사건에서 실제로 필요한 조문 전체를 커버하지 못하고 있어, 점진적 보강이 필요하다.

### AI 검색 구조 개선
- 현재는 키워드 기반(문자열 유사도) 검색만 사용 중이며, 의미 기반 검색(임베딩)은 비용 대비 실익이 아직 명확하지 않아 도입을 보류하고 있다. 키워드 검색의 한계(동의어·문맥 검색 실패)가 실제로 누적되면 그때 재검토하기로 되어 있다.
- 검증 과정에서 반복적으로 실행되는 대조 쿼리 패턴을 재사용 가능한 형태로 정리하면, 매 검증마다 쿼리를 새로 설계하는 비용을 줄일 수 있다.

### 자동화 가능 영역
- 신규 자료 반입 시 지문 대조·대장 등재는 이미 자동화되어 있으나, **1차 판독 자체**(텍스트 추출 시도 → 실패 판정)까지는 자동화되고, 그 다음 단계(이미지 판독)부터는 AI 판단이 개입하는 구조를 더 명확히 분리할 수 있다.
- 발송 전 4중 검사(수신자 적합성/렌더링 부산물/금지어/가독성) 중 금지어 검사는 이미 완전 자동화되어 있고, 나머지 항목도 상당 부분 규칙화가 가능해 보인다.
- 독립 검증 보고서의 5개 고정 항목(판정/치명적 결함/주의사항/표본 내역/미확인 항목) 형식을 강제하는 것은 이미 되어 있으나, 이 형식 자체를 반자동으로 채점(예: "치명적 결함 0건 여부"를 별도 파싱)하는 후처리는 아직 사람이 읽고 판단한다.

---

## 부록: 이 문서 자체에 대한 메모
이 문서는 외부 AI 검토를 위해 **의도적으로 일반화**되었다. 다음은 이 문서에서 생략한 것들이다.
- 실제 데이터베이스 테이블/컬럼의 정확한 이름과 스키마 전문
- 조직 구성원 명칭 체계, 사건 식별 번호 체계
- 사건 당사자·상대방 기관의 실명, 사건의 구체적 내용
- 배포 파이프라인의 정확한 파일 목록(화이트리스트 원문)
- 접근 통제(인증·권한) 메커니즘의 상세 구현

실제 아키텍처 개선을 논의할 때는 이 문서를 출발점으로 삼되, 세부 스키마나 보안 메커니즘까지 필요한 논의는 접근이 통제된 환경 내에서만 진행하는 것을 권장한다.
